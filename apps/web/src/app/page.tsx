/**
 * Placeholder del scaffold — sin datos, sin `conSesion` (no aplica: esta página no lee nada).
 * El login real y el wizard llegan en piezas siguientes de este mismo paso revertible (PR4).
 */
export default function Home() {
  return <p>Sistema Contable — en construcción.</p>;
}
