# Registro de incidentes de seguridad

> **Procedimiento completo: `docs/arquitectura/ADR-0002-seguridad.md` §E.4.** Este archivo es la
> bitácora, no el procedimiento.
>
> **Orden no negociable: contener primero, entender después.** Rotar o revocar en el minuto uno; no se
> investiga con el secreto vivo. Si es una credencial fiscal, la revocación puede requerir una acción
> **del cliente** ante el organismo recaudador: contactarlo es parte del paso 1.

## Incidentes

| # | Fecha | Qué se filtró / qué pasó | Ventana de exposición | Alcance (clientes) | ¿Hay evidencia de uso? | Acciones y horarios | Control que impide la repetición | ¿Cerrado el CONTROL? |
|---|---|---|---|---|---|---|---|---|
| **1** | 2026-08-15 | **Vulnerabilidad de aislamiento — exposición NO confirmada.** Escalada de privilegios por shadowing de `pg_temp`: 6 de las 8 funciones de `app` leían relaciones sin calificar el esquema, y `pg_temp` se busca primero aunque no esté en el `search_path`. Con la credencial ordinaria de la aplicación (sin superusuario ni `BYPASSRLS`) se anulaban `accessible_tenant_ids()` y `has_role_on()`, o sea **lectura y escritura sobre todos los tenants de la instancia**. Al caer `has_role_on`, **N2-R quedó al nivel de N2**. NO se expuso `credencial_fiscal.material_cifrado` (N3): su control es un grant por columna, que se evalúa antes que la RLS | **Desde `0001_tenancy.sql` (2026-08-10) hasta el fix (2026-08-15).** El defecto era fundacional: existió cada minuto de vida del esquema | 2 bases. `sistema_contable` (local, sintética). `sistema_contable_piloto`: los titulares reales bajo la excepción E-1, en tenants separados. *(Sin nombres, uuid ni identificadores en esta bitácora.)* «Es local» no baja la severidad: E-1 fija que el entorno local se trata como productivo a los efectos de los controles | **No se puede determinar** — y eso es un hallazgo, no un detalle (regla 2). `acceso_auditoria` se escribe **desde la aplicación** y el vector no pasa por la aplicación; Postgres no tiene triggers de `SELECT`, `pgaudit` está descartado (ADR-0000 §6) y `log_statement` está en el default. Lo que **sí** se verificó, antes y después del fix: `verificar_coherencia_path()` = 0 y cero membresías inesperadas en las dos bases ⇒ **no hay evidencia de escalada persistente** (≠ «no hubo acceso»). 🔴 **ANOTACIÓN 2026-08-16 — esa segunda medición quedó DEGRADADA como evidencia, por el incidente #5.** «Cero membresías inesperadas» se lee sobre `membership`, y el #5 establece que esa tabla es **editable y borrable por el propio sujeto auditado, sin dejar fila**: la policy no restringe la fila que se toca, el grant es de tabla entera y no hay rastro de escritura. Un `DELETE` no deja tumba y un `update activo = false` no deja versión. `verificar_coherencia_path()` **sigue en pie** como medición independiente; la de membresías **no prueba lo que se creyó que probaba**. No se reabre el incidente: se anota con precisión, porque el enunciado «no hay evidencia de escalada persistente» sigue siendo correcto —lo que cambia es cuánta fuerza tiene la mitad de la evidencia que lo sostiene | Encontrado por auditoría interna de un agente, revisando otra cosa. Reproducido 3 veces de forma independiente. Barrido de las 8 funciones. Verificación forense **antes** de parchear (el vector permitía una membresía real que habría sobrevivido al fix). `0015_search_path_pg_temp.sql` aplicada a local y piloto el 2026-08-15, hash verificado sin drift. PoC re-ejecutado post-fix por las dos vías | **R10 + R10 bis + R10 ter**, ADR-0002 §B.1, **reescritas dos veces el mismo día**. Final: la **primera** aparición de `pg_temp` es la **última posición** y hay ≥2 elementos, sobre `prokind in ('f','p')` de `app`/`public`, `definer` **e** `invoker`, con la exención nominada por esquema+nombre+aridad; ningún rol **no superusuario** conserva `TEMPORARY`; `app`/`public` son los únicos esquemas de dominio. **Dos versiones anteriores pasaron verde con el bug adentro**: la original («fija `search_path`» — las vulnerables lo fijaban) durante los cinco días de vida del esquema, y la primera corrección («**terminado** en `pg_temp`») durante un commit — PostgreSQL resuelve por **primera** aparición, así que `pg_temp, public, app, pg_temp` y `pg_temp` a secas terminaban en `pg_temp` y leían la trampa igual. Cerrado con **40 mutaciones**, 10 de ellas verdes indebidas al empezar, 0 al final, más `pg-temp-shadowing.test.ts`: la primera cobertura **conductual** del vector — planta la trampa de verdad, con control de vacuidad exacto, y se pone roja ante las 7 reversiones del esquema | **SÍ** — 2026-08-15. Código aplicado y verificado en las dos bases (PoC bloqueado por las dos vías, por separado) y control cerrado recién en la **tercera** redacción de la regla, la primera que sobrevive a la prueba por mutación. **La primera marca de cerrado fue prematura**: se puso con la regla ya escrita pero sin mutar, que es exactamente el error que este incidente documenta |
| **2** | 2026-08-15 | **Vulnerabilidad de aislamiento — exposición NO confirmada.** Ruptura del árbol de tenancía por edición manual de `tenant_node.path`. El trigger `app.rechazar_path_manual()` (`0001_tenancy.sql:131-144`) deja pasar la edición cuando el GUC `app.reparentando` vale `'on'` — y ese GUC es un *customized option* de namespace, que **cualquier sesión puede prender** con `set_config`, sin privilegio asociado. No es un control de autorización: es una bandera que el propio atacante enciende. La policy `tenant_node_wr` (`0001:322-327`) valida **quién** escribe y **qué padre** declara, nunca **el valor** del `path`. Como `app.accessible_tenant_ids()` resuelve el subárbol **por path**, un `path` fabricado mueve un nodo de subárbol a efectos de RLS. Es **H-1 reabierta por la puerta que el propio trigger dejó**: el comentario que precede a ese trigger describe esta misma fuga con todas las letras. Agravante: `app.reparentar_nodo()` aborta si el árbol queda incoherente; por esta vía el `path` corrupto **queda escrito**. 🔴 **Cae N2 y también N2-R** — y por un mecanismo distinto del #1: `has_role_on()` resuelve **por path** (`tn.path like mn.path \|\| '.%'`, `0001:233-246`), así que **no se anula: se satisface legítimamente**. El socio ajeno pasa a tener rol sobre ese nodo, con `cuenta_bancaria_identificador.numero` (CBU entero), `movimiento_origen_crudo.fila_origen` (la fila cruda, con CUIT de terceros que no son clientes de nadie) y `padron_socio_documento.documento`. **N3 no cae**, por la misma razón que en el #1: el grant por columna de `0002:114-118` no depende del árbol. **Y no es solo lectura**: `mov_crudo_wr` admite `socio` con `update`/`delete` —integridad del registro contable— y `membership_wr` le permite **crearse una membresía sobre el nodo empujado, que sobrevive a corregir el `path`**. Segundo orden: el `acceso_auditoria` del cliente empujado queda legible por el estudio ajeno, o sea que el rastro que existe para responder un incidente **se vuelve material del incidente** | **Desde `0001_tenancy.sql` (2026-08-10) hasta hoy — ABIERTA.** Fundacional, igual que el #1, y del mismo archivo | 2 bases, las mismas del #1. *(Sin nombres, uuid ni identificadores en esta bitácora; el PoC corrió en una base descartable con topología sintética, borrada al terminar.)* **Requiere rol `socio` o `admin_plataforma`** (un `contador` recibe `UPDATE 0`) — **y eso NO es una atenuante**: las filas 1 y 2 del modelo de amenaza de ADR-0002 §0 son insiders con cuenta legítima («estudio competidor», «empleado que se va»), o sea que el perfil es **la premisa**, no la excepción. Además `admin_plataforma` es un rol que H-9 ya tiene marcado para sacar de las policies de escritura. 🔴 **Y la contraseña de la credencial de aplicación está commiteada** en `.env.example`: mientras no se rote, el atacante no necesita comprometer una cuenta de socio, le alcanza con **conocer un uuid de socio** (`app.user_id` es un GUC que él mismo setea). Dirección **«push», no «pull»**, que **tampoco baja la severidad**: el sujeto protegido por el secreto fiscal es **el cliente**, no el estudio, y en push el damnificado es el único de los tres que no controla ningún control. Push es **superconjunto** de pull, no subconjunto: agrega escritura, agrega persistencia y **el estudio legítimo pierde acceso a su propio cliente**. La inversa (capturar un cliente ajeno) falla hoy por la coincidencia de tres cosas —`with check parent_id is not null`, **R15** (cada estudio es raíz ⇒ su path no tiene prefijo propio posible) y **`uq_tenant_node_path`**—, o sea que **ese índice único es hoy un control de aislamiento y en ningún lado figura como tal**: quien lo relaje «para permitir mover nodos» reabre el pull sin enterarse | **A diferencia del #1, acá SÍ hay mecanismo de detección** y dio limpio: `app.verificar_coherencia_path()` = **0 incoherencias en las dos bases**, medido el 2026-08-15 en el forense del #1 ⇒ **no hay evidencia de que se haya ejecutado** (≠ «no se ejecutó»). El vector deja rastro **observable mientras está en efecto, no indeleble** —un `path` que no coincide con el computado—; `verificar_coherencia_path()` detecta un **estado**, no un **evento**, y el estado es reversible. **Se suma la otra medición del mismo forense: cero membresías inesperadas en las dos bases**, y esa es la que cubre el residuo persistente de este vector. 🔴 **ANOTACIÓN 2026-08-16 — y es justamente la que quedó DEGRADADA por el incidente #5**, que pega más fuerte acá que en el #1: el residuo persistente de ESTE vector es una `membership`, y el #5 establece que un `socio` puede desactivarla o borrarla **sin dejar rastro**. O sea que la medición que cubre el residuo corre sobre un estado que el atacante puede limpiar. No se reabre el incidente: se anota, y el peso de la afirmación baja en consecuencia. No es un agregado cosmético: revertir el push **no es simétrico** —tras empujar el nodo, el atacante lo pierde de su propio `accessible_tenant_ids()` y el `using` de `tenant_node_wr` ya no lo deja volver—, así que la variante «corrió y revirtió sin dejar nada» **exige un paso previo que sí deja fila**: una `membership` sobre el nodo. Lo que **no** hay es rastro del **intento** ni fecha. 🔴 Y **`tenant_node.updated_at` no sirve como evidencia forense**: en este vector el `UPDATE` lo escribe el atacante, que puede omitir la columna o poner lo que quiera | Encontrado por `tester` en la ronda de cierre del #1, atacando el estado post-fix — no por el barrido del #1, que buscaba otra clase de bug. Reproducido punta a punta en base descartable: el socio del estudio ajeno pasó de 1 a 3 nodos visibles y leyó `movimiento_bancario_crudo` del cliente empujado. Mecanismo confirmado contra el código de forma independiente. `verificar_coherencia_path()` re-corrido en las dos bases: 0. **Barrido de la clase entera** (¿hay otro control que dependa de un valor que el invocador setea?): en todo el repo hay **exactamente dos** GUC de namespace — `app.user_id` y `app.reparentando` — y **no hay un tercero**; `app.reparentando` no se setea nunca desde TS. La distinción que faltaba escrita: un GUC puede transportar **identidad** (`app.user_id`: no concede nada por sí mismo, todo sale de filas de `membership`, y ante valor ausente falla cerrado), **nunca autorización**. **Contención pendiente, no aplicada:** rotar la credencial de aplicación, y `revoke update on tenant_node from app_request` — medido: **cero** `update` sobre `tenant_node` en todo el TS, así que es privilegio que hoy nadie ejerce | **R36**, ADR-0002 §B.1, **enunciada sobre el PREDICADO y no sobre el mecanismo** — que es la forma que falló en R33 y en la primera redacción de la propia R36. El `path` es una **función** de `(parent_id, nid)`, verificada sobre el **estado físico**, sin excepción por rol, GUC, vía de escritura ni `BYPASSRLS`. Aplicada por `0017_path_por_construccion.sql` (local y piloto, hash `d35ba671b8c2b66b` verificado en las dos puntas) más `0018_parent_id_solo_en_el_alta.sql` (local). 🔴 **El invariante baja de trigger a `check` + `foreign key`**, porque es **referencial** y Postgres exime a `check`/`unique`/`foreign key` de la RLS **por diseño** — los triggers no. Se eligió esa forma sobre `security definer`: **R11 no se toca y no hizo falta ningún ADR**. 🔴 **Y hay que decir que `0016` atribuía el cierre a la rama equivocada:** está medido que el ataque original lo frenaba la **comparación de coherencia**, no la rama `not found` — que nunca fue un control sino una **compensación por una ceguera autoinfligida** (el trigger era `invoker`). El corolario, que vale más allá de esta tabla: **un invariante verificado con la visibilidad del escritor no es un invariante**. **Los defectos del propio fix, y el vector de `nid` —que resultó fundacional como éste— se registran aparte, en el #4**, con el mismo criterio con que el #3 se abrió aparte pese a haber surgido como contención de éste | **SÍ, CON ALCANCE DECLARADO** — 2026-08-15/16. Regla numerada y reescrita sobre el predicado; mecanismo aplicado y verificado en las **dos** bases; probado **con los tres roles** —`app_request` mide el privilegio, `app_job` (BYPASSRLS) y el **dueño** miden el invariante—, porque un ataque probado sólo con `app_request` **se pone verde el día que alguien re-otorgue el grant de tabla entera** de la plantilla de ADR-0001 §5. Verificado **que DISPARA, no que existe**: sonda con un nodo **nuevo** en una transacción que siempre revierte. 🔴 **Se cierra el VECTOR original, no el bien protegido**: la coherencia del árbol sigue teniendo residuos declarados, y viven en el **#4**. 🔴 **Y R36 queda en ⚠️, no en ✅**: su ronda de mutación dejó **7 de 27 vivas** y aparecieron dos hallazgos ALTA. Marcarla verde ahora sería repetir exactamente lo que este registro documenta dos filas más arriba |
| **3** | 2026-08-15 | **Credenciales de base commiteadas en un repositorio PÚBLICO.** `.env.example` está **trackeado** por git (no está en `.gitignore`; es el único `.env*` trackeado) y lleva valores reales, no marcadores. Al comparar variable por variable contra los `.env*` vivos resultaron **9** coincidencias, no las 4 de la instrucción inicial: `POSTGRES_PASSWORD`, `APP_DB_PASSWORD`, `JOB_DB_PASSWORD`, `MINIO_ROOT_PASSWORD`, los dos pares de claves S3 y `CRON_SECRET`. **`IDENTIFICADOR_PEPPER` NO coincidía** — se generó por entorno, que es la razón por la que el HMAC de identificadores no es correlacionable entre entornos. Las dos que importan **no** son la de la aplicación: **`POSTGRES_PASSWORD` es el dueño del esquema, que además es superusuario en las dos bases** —ignora la RLS siempre, forzada o no, y no necesita ningún vector— y **`JOB_DB_PASSWORD` tiene `BYPASSRLS`**. Contra cualquiera de esas dos, todo el mecanismo de aislamiento es irrelevante: no hay control de RLS que aplique. `app_request` es, de las cuatro, la **menos** grave. *(Ningún valor se transcribe acá: se nombran las variables.)* | **Desde `ff2d992` (2026-08-10), commit que está en `origin/main`. El uso futuro se cortó el 2026-08-15 con la rotación; la EXPOSICIÓN no se cierra nunca.** Por la **regla 4 de esta bitácora, la exposición no se cierra rotando**: un secreto commiteado se considera público para siempre. Rotar corta el uso futuro; el pasado no se puede cerrar | **Los dos entornos.** El **piloto** (material real bajo E-1) y **local**, cuyo `.env` tenía las mismas credenciales commiteadas hasta que se rotó también ahí — y por E-1 el entorno local se trata como productivo a los efectos de los controles. Verificado sin transcribir valores: `POSTGRES_PASSWORD`, `APP_DB_PASSWORD`, `JOB_DB_PASSWORD` y `APP_DB_USER` de `.env.piloto` son **idénticas** a las commiteadas, o sea que **no quedaron obsoletas por ninguna de las reconstrucciones**. Confirmado además por autenticación real: la credencial publicada **abre sesión hoy** contra la base del piloto | **No se puede determinar** si alguien clonó o indexó el repositorio, y no hay forma técnica de saberlo desde acá. **Mitigante medido, y es la única razón por la que no es una emergencia del minuto:** el piloto se publica **solo en loopback** (`127.0.0.1:5443`, `docker-compose.piloto.yml:60`; el comentario del archivo declara la intención). **La credencial es pública; el puerto no** ⇒ se requiere acceso a la máquina. **No** se afirma que no se haya usado | Encontrado al verificar, a pedido del titular, tres preguntas puntuales sobre una afirmación anterior — que la credencial estaba commiteada, traída al principio como simple contención del #2. Las tres respuestas (trackeado / llegó al remoto / sigue vigente) convirtieron una nota al margen en un incidente propio, **más grave que los dos anteriores**. Dato lateral que salió del mismo chequeo y va a favor: `APP_ENTORNO` en el piloto es `piloto`, no `local`, así que el guard de arranque de `conUsuario()` está activo ahí | **R37 + R37 bis**, ADR-0002 §B.3, que **reemplazan a R33**. R37 barre **`git ls-files`** —lo trackeado, que es lo único que puede viajar a un remoto— y prohíbe tres formas: archivo con forma de entorno, URL con credenciales embebidas, y clave `UPPER_SNAKE` secreta asignada a un literal que no sea marcador. 🔴 **Y ningún permitido exime del cruce con los `.env*` vivos**: un literal que además sea credencial de un entorno real es violación siempre — que es el #3 en una línea. R37 bis prohíbe la negación en `.gitignore` que fue el vector. **R33 ya decía «ningún secreto en el repo» y nombraba en su propio enunciado la excepción que falló** (*«salvo `.env.example`»*), con la condición *«solo nombres»* que nadie chequeaba, y su campo de estado admitía la violación por escrito. Al correrse por primera vez, R37 encontró credenciales en **cuatro archivos más**, ninguno llamado `.env` | **SÍ** — 2026-08-15. Las 9 credenciales rotadas y verificadas en las dos direcciones contra lo que hay hoy en `origin/main`; `.env.example` fuera del tracking y publicado; regla numerada con el porqué de su redacción; y el gate cerrado **por mutación** (3 contra el repo real, 7 sintéticas). 🔴 **El cierre es del CONTROL, no de la exposición**: por la regla 4, lo que estuvo en un repo público desde el 2026-08-10 estuvo, y eso no se revierte |
| **4** | 2026-08-15 | **Defectos del control de integridad del árbol — exposición NO confirmada.** El fix del #2 (`0016_path_coherente.sql`) implementó el invariante como un `constraint trigger` diferido **`invoker`**, que **re-leía la tabla BAJO RLS**. De ahí los defectos, y la lección que los engloba: **un control de integridad que corre bajo RLS hereda la ceguera de la RLS.** **(A)** `nid` **no está en la lista de columnas de NINGÚN trigger** — ni de `0016:148` ni de `0001:115/127/143`. Es `generated always as identity`: no admite literal **pero sí `DEFAULT`**. Medido como socio ordinario: `update … set nid = default` **commitea** y deja el árbol incoherente (multi-fila y `MERGE` también); `set nid = default, parent_id = parent_id` es **peor**, porque el trigger de `0001:127` recalcula el `path` con el `nid` nuevo, el resultado sale **coherente**, y el `path` quedó reescrito **sin `reparentar_nodo()` y sin el GUC** — o sea que la afirmación de `0001:130` (*«la única vía de reescritura es `app.reparentar_nodo()`»*) **era falsa**. Y `insert … overriding system value` con `nid` explícito **mina el secuencial GLOBAL**, haciendo fallar altas de **otros** estudios. **(B)** El barrido de hijos de `0016:128-138` **FALLA ABIERTO**: re-lee los hijos bajo RLS y trata `not found` como **no-op** — el mismo bug que el encabezado de `0016` documenta con todas las letras cuarenta líneas más arriba. Mudar un padre a un path coherente **consigo mismo** dejaba al hijo colgado y **commiteaba**; con el hijo escondido antes por `deleted_at`, también. **(C)** La rama `not found` confunde tres situaciones y sólo una es un ataque —el atacante se auto-ocultó la fila, la fila se **borró** en la misma transacción, baja lógica—: **(b) no tiene nada que ver con RLS** (se reproduce como dueño superusuario) y **abortaba operaciones legítimas**; dejó **ROJO** `packages/data/sql/tests/0001_aislamiento.test.sql`, que corre en CI. **(D)** Y una vía nueva, encontrada sobre el estado post-`0017`: `app_request` tenía grant sobre `parent_id`, y como el trigger **recalcula el `path` desde el padre**, un solo `update … set parent_id` **relocalizaba un subárbol entero** —cuatro niveles, coherente, sin el GUC, sin `reparentar_nodo()`, **sin una sola fila en `acceso_auditoria`**—. Un `contador` con membresía sólo sobre un grupo pasó de ver 2 nodos a ver **3**. Y era **no determinístico**: dependía del orden físico del heap. **(E) Regresión de R25/R28 introducida por el fix del fix:** al bajar el invariante a `check`/`foreign key`, el texto del error lo genera Postgres y **no se puede customizar**; con `app_job` o el dueño sale `DETAIL: Failing row contains (…)` con `nombre` (N2) y `path` (N1, `exportable:false`), **al log del servidor** — fuera de la RLS y fuera del redactor. Medido: **203 ocurrencias ya en el log local**, y **no hace falta un ataque**: dos `reparentar_nodo()` concurrentes lo producen | 🔴 **Tres ventanas distintas, y por eso se escriben las tres.** **B, C: desde `0016` (2026-08-15) hasta `0017` (2026-08-15) — horas, cerradas el mismo día.** **A y D: desde `0001_tenancy.sql` (2026-08-10) — FUNDACIONALES, la misma ventana que el #2, distinto vector**; no son defectos del fix, se **encontraron** en su ronda de cierre. **E: desde `0017` (2026-08-15) — ABIERTA** | 2 bases, las mismas del #1, #2 y #3. *(Sin nombres, uuid ni identificadores; los PoC corrieron con topología sintética en bases descartables.)* **A, D y E requieren rol `socio`/`admin_plataforma`, `app_job`, o el dueño** — y por las filas 1 y 2 del modelo de amenaza de ADR-0002 §0, el insider con cuenta legítima es **la premisa, no la excepción**. **B y C se disparan sin privilegio especial**: C rompía una operación legítima cualquiera. El alcance de A es el del #2 (cae N2 y N2-R por resolución de `has_role_on()` por path; **N3 no cae**, por el grant por columna de `0002:114-118`) **más uno que el #2 no tiene: denegación de servicio cruzada** — un `nid` minado hace fallar el alta de un estudio ajeno. D **no es cross-tenant** (el `with check` confina el padre nuevo al subárbol accesible) pero **sí es una reasignación de visibilidad interna que nadie autorizó** | **A: el mismo mecanismo del #2, y dio limpio** — `verificar_coherencia_path()` = **0 en las dos bases**, antes y después de `0017` ⇒ **no hay evidencia de que se haya ejecutado** (≠ «no se ejecutó»). 🔴 **Con dos límites que hay que escribir:** detecta un **estado** reversible, no un evento; y la variante `set nid = default, parent_id = parent_id` **deja el árbol COHERENTE**, así que `verificar_coherencia_path()` **es ciega a ella por construcción** — para esa variante **no hay ningún mecanismo de detección**, ni lo hubo. **D: tampoco deja rastro** — el único vestigio es `updated_at`, que el propio escritor controla. **B: no se puede determinar**; el fallo abierto no deja nada distinguible de una operación normal. **C: sí hay evidencia, y es un test en rojo en CI** — que es la única razón por la que se encontró. **E: no aplica** (es una vía de fuga, no un acceso) | Encontrado por la **ronda de cierre de `0016`** y por la de `0017` —`dba-data`, `qa-automation`, `tester`, `security-engineer`, `seguridad-datos-financieros`—, atacando el estado post-fix, con el mismo patrón que encontró el #2 en la ronda del #1. **La cifra «4 mutaciones, 4 atrapadas» de `0016` era engañosa: eran las 4 que el test fue escrito para atrapar**; se corrieron **9 más y sobrevivieron 8**, y su caso 5 era una **tautología**. Sobre `0017` se corrieron **27 mutaciones y sobrevivieron 7**, todas en la familia declarativa que ningún test previo miraba. `0017` aplicada a local y piloto (hash verificado en las dos puntas, `verificar_coherencia_path()` = 0 antes y después, sonda que confirma que **dispara**); `0018` aplicada a **local** — **al piloto NO, espera confirmación explícita del titular** | **R36** (ADR-0002 §B.1) en su **tercera** redacción, la primera enunciada sobre el **predicado**. Cierra **A** (`tenant_node_path_chk` + `trg_tenant_node_nid_inmutable`, que es **mecanismo** y por eso alcanza también al dueño), **B** (`tenant_node_parent_path_fk`, `match full`, diferida: **no pasa por la RLS, así que no puede fallar abierta**), **C** (desaparece la rama `not found`; los dos falsos positivos commitean y se sacó el parche temporal del test de CI) y **D** (`0018`: `app_request` conserva `INSERT` sobre `parent_id` —lo necesita el alta— y pierde el `UPDATE`; mover un nodo queda en `reparentar_nodo()` con `app_job`, que es lo que ADR-0001 §8.2 declaraba desde el principio). 🔴 **Lo que R36 NO cierra, y queda declarado:** el minado del secuencial global vía `overriding system value` —cualquier `nid` es **íntegramente válido**, no hay constraint que lo rechace—; lo cierra el **privilegio**, y el privilegio **no le aplica al dueño**. **E no tiene regla, y por eso el incidente no se cierra** | **NO.** 🔴 **A, B, C y D están cerrados; E no.** Falta la regla verificable numerada para el `DETAIL` del driver. **Corrección de rumbo:** las dos mitigaciones que se declararon pendientes —mapear `23514`/`23503` y que el logger no emita `detail`— **ya estaban en el código desde `ff2d992`** (`errores-pg.ts:77-82`; `conErroresTraducidos` **descarta** `detail`/`where`/`hint`/`parameters`; `logger.ts:112`, `redactar.ts:188`). **El hueco real es otro:** `conUsuario`/`conJob` **re-lanzan el error crudo del driver** (`conexion.ts:255`, `:289`), así que la protección existe **por callsite y no por diseño**. Hoy no hay vía alcanzable desde la aplicación — pero eso es **una propiedad del código de hoy, no un control**, y se rompe con el primer llamador nuevo |
| **5** | 2026-08-16 | **Vulnerabilidad de supervisión y trazabilidad — exposición NO confirmada.** El padrón de derechos es escribible por el sujeto del control, sin restricción de fila y sin rastro. **Tres ausencias que se componen, y cerrar una sola deja el hallazgo vivo:** **(a)** `membership_wr` (`0001:335-339`) es `for all` y gatea **sólo por el nodo** — **ni `using` ni `with check` mencionan `rol` ni `activo`**, o sea que el predicado pregunta por el rol de **quien escribe** y nunca por el rol de **la fila que se toca**; **(b)** el `grant` es de **tabla entera** (`0001:342` a `app_request`, `0001:349` a `app_job`), sin recorte por columna — a diferencia de `credencial_fiscal` (`0002:114`) y de `tenant_node` (`0017`/`0018`); **(c)** **no hay rastro de escritura**: `acceso_auditoria` registra **acceso**, no **cambios de derecho**, y ninguna migración engancha un trigger de auditoría a `membership`. Efecto medido, actuando como `socio` del propio estudio: `update membership set activo = false where rol in ('auditor','admin_plataforma')` → **UPDATE 2**, y `delete from membership where rol = 'auditor'` → **DELETE 1**. El `auditor` pasó de árbol completo y 1 fila de auditoría a **0 y 0**; el `admin_plataforma`, a **0 nodos**. `rol` sólo permite **degradar**; **`activo` y el `DELETE` expulsan la supervisión entera** — al auditor del estudio y al staff de la plataforma, que **no es personal del estudio** (`0001:36`). El `DELETE` además borra el `created_at` y **no hay `revocado_en` ni historial**: se pierde **el hecho de que esa supervisión existió**, que es justo la pregunta que se hace después de un incidente. 🔴 **Y tiene consecuencia sobre las filas #1 y #2 de esta misma bitácora**: la medición forense *«cero membresías inesperadas en las dos bases»*, que sostiene el *«no hay evidencia de escalada persistente»* de las dos, corre sobre un estado que **el sujeto auditado puede editar y borrar sin dejar fila**. `verificar_coherencia_path()` sigue en pie como medición independiente; **la de membresías queda degradada como evidencia** — anotado en las dos filas, sin reabrirlas. **No cae aislamiento entre clientes**: el vector opera dentro del propio subárbol y ninguna N2 ni N2-R de otro estudio se alcanza por esta vía. Severidad: **ALTA en trazabilidad y supervisión, MEDIA en confidencialidad** | **Desde `0001_tenancy.sql` (2026-08-10) hasta hoy — ABIERTA.** Fundacional, y **sin modificación en las diecisiete migraciones siguientes**, verificado elemento por elemento: `0005_policies_sin_for_all.sql` cierra la clase `for all` **sólo en `credencial_fiscal`** y deja `membership` afuera; `0015` reescribe `has_role_on()` por el `search_path` **sin tocar su predicado**; `0017`/`0018` aplican grant por columna a `tenant_node` y **no a `membership`**. `0016:21` **nombra de costado la mitad aditiva** del defecto (*«`membership_wr` le permite crearse una membresía…»*) en contexto del #2; la mitad **sustractiva** —desactivar y borrar la ajena— no está escrita en ningún lado, y esa mención **no acota la ventana** | 2 bases, las mismas del #1 y del #2. *(Sin nombres, uuid ni identificadores en esta bitácora.)* Requiere rol `socio` o `admin_plataforma`, **y eso NO es atenuante**: las filas 1 y 2 del modelo de amenaza de ADR-0002 §0 son insiders con cuenta legítima — mismo criterio ya aplicado en el #2. `app_job` (BYPASSRLS) tiene además el grant completo, así que para ese rol **ni siquiera media una policy**. 🔴 **Alcance a la plataforma, no sólo al estudio:** expulsar a `admin_plataforma` deja al operador del SaaS **sin capacidad de intervención sobre un tenant que sigue custodiando datos fiscales de terceros** — y esos terceros, los clientes del estudio, no son parte de esa relación ni la pueden observar. **Empuja en contra de INV-11 y de H-9**, que ya tenían marcado el ciclo de vida de la membresía de soporte como pendiente | **No se puede determinar, y es un hallazgo, no un detalle (regla 2).** 🔴 **Es la peor de las cinco filas en este campo:** en el #1 faltaba el evento; en el #2 al menos quedaba un **estado** observable y un residuo persistente. Acá **el objeto que se destruye ES el rastro**: un `UPDATE` de `activo` no deja versión anterior, un `DELETE` no deja tumba, `membership` no tiene `updated_at` ni `revocado_en`, y `acceso_auditoria` **no puede alojar el evento** — su trigger `trg_acceso_auditoria_cliente` exige un nodo de tipo `cliente` activo, y una membresía de `socio`/`auditor`/`admin_plataforma` cuelga típicamente del nodo **estudio**: la fila **no entra**. Lo único verificable hoy es **el estado presente del padrón**, y sólo contra una expectativa escrita **fuera** de la base. **No se afirma que el vector no se haya ejecutado** | Encontrado por `tester` en la ronda de cierre de `0017`, atacando el estado post-fix — mismo patrón de descubrimiento que produjo el #2 desde el #1 y el #3 desde una contención del #2. Mecanismo confirmado contra el código de forma independiente por `seguridad-datos-financieros`: policy, grants y ausencia de auditoría verificados línea por línea, y ventana medida contra las 17 migraciones. **Medición que acota la contención**, con el mismo método que el #2 usó sobre `tenant_node`: **cero** `insert`/`update`/`delete` sobre `membership` en código de producción TypeScript — los únicos están en el seed sintético y en helpers de test; el CLI de alta y baja de socio opera sobre `padron_socio`, que es otra tabla. O sea que **el `UPDATE`/`DELETE` de `app_request` sobre `membership` es privilegio que hoy no ejerce ningún camino de aplicación**. 🔴 **Restricción de diseño que toda remediación debe respetar:** ADR-0002 **INV-10** exige que revocar una membresía corte el acceso en el request siguiente — la escritura sobre `activo` **tiene que seguir existiendo**; el defecto no es que se escriba, es **sobre qué fila, con qué grant y sin qué rastro** | **PENDIENTE.** 🔴 **`0019_membership_supervision.sql` está construida, aplicada a LOCAL —nunca al piloto— y VOLVIÓ A TALLER en su ronda de cierre: cierra el ataque LITERAL, no la clase.** Las tres patas están puestas y verificadas —policy que mira el rol de la fila tocada, grant por columna, rastro append-only escrito por trigger— y el ataque original queda bloqueado en todas sus formas (`MERGE`, `ON CONFLICT`, CTE, `truncate`, update masivo). Pero la ronda dejó **11 de 28 mutaciones vivas** y **dos bloqueantes reproducidos**: 🔴 **`tenant_node.deleted_at` reproduce el resultado entero sin tocar `membership` y sin dejar rastro** —el socio expulsa al supervisor **y le apaga la vista del rastro** en una sentencia, porque `accessible_tenant_ids()` exige `deleted_at is null` sobre el nodo de la membresía; medido: el auditor pasa de 2 filas de rastro y 1 nodo a **0 y 0**, con las filas **intactas en disco**—; y 🔴 **el rastro es escribible por cualquiera con acceso al nodo** —`socio`, `administrativo`, `cliente_lectura` y **el propio `auditor`**— con filas **indistinguibles de las del trigger**. Más: `app_job` conserva un `grant update (activo)` que **no usa ningún camino de producción** y con el que expulsa a la supervisión **de todos los tenants en una sentencia**, dejando `hecho_por = null` — **el mismo valor que el nulo legítimo**, o sea indistinguible de una migración. **Plan formal propio** con `dba-data` + `seguridad-datos-financieros` + `security-engineer`: la corrección de fondo —que el rastro lo escriba una función `security definer` en vez de depender de un grant abierto al tenant— **choca con R11 y es decisión de arquitectura**, no un parche. Las tres patas juntas: predicado de rol **de la fila tocada** + grant por columna sobre `activo` y `rol` + **rastro de cambio de derecho**. 🔴 **Ese rastro NO puede ir contra `acceso_auditoria`**, y el motivo es mecánico antes que semántico: su trigger rechaza la fila porque una membresía de estudio no tiene `cliente_id` de tipo `cliente`. Va en tabla propia, append-only, **escrita por trigger y no por la aplicación** — que es la lección del #1 en una línea: *un rastro que depende de que el escritor coopere no sirve contra un escritor hostil*. Precedente en el repo: `credencial_fiscal` + `credencial_fiscal_rotacion` | **NO** — regla 3: no se cierra sin la regla verificable numerada en ADR-0002 §B, y —lección del #1— **no se marca cerrada antes de pasar la prueba por mutación** |

> 🔴 **ADVERTENCIA PROBATORIA — vigente mientras `0019` no se rediseñe (2026-08-16).**
>
> **Una fila de `membership_historia` NO es evidencia confiable por sí sola: pudo haber sido
> fabricada.** El `insert` sobre esa tabla está otorgado a `app_request` sin mirar el rol, así que
> **cualquier identidad con acceso al nodo** —`socio`, `administrativo`, `cliente_lectura` y **el
> propio `auditor`**— puede escribir filas **indistinguibles de las que escribe el trigger**:
> misma tabla, mismas columnas, y `hecho_por` puesto por el mismo `DEFAULT`. **No hay ninguna
> columna que diga «esto lo escribió la base».** Medido en la ronda de cierre de `0019`, incluida
> una fila fabricada que afirma que se borró la membresía de un auditor — una operación que `0019`
> volvió imposible.
>
> **Y la ausencia de una fila tampoco prueba nada:** `tenant_node.deleted_at` esconde el rastro de
> un nodo entero de la vista de quien supervisa, sin borrar una sola fila del disco.
>
> **Qué SÍ se puede hacer con este rastro, hoy:** reconstruir de buena fe una secuencia de cambios,
> y usarlo como **indicio** que hay que corroborar contra otra fuente. **Qué NO:** sostener una
> imputación contra una persona, ni afirmar que un cambio de derecho no ocurrió porque no figura.
>
> 🔴 **Esto NO afecta al control.** El ataque del incidente #5 —expulsar a la supervisión— queda
> bloqueado **de forma independiente**: medido en nueve escenarios, con y sin filas fabricadas, en
> transacciones separadas y en la misma, los supervisores quedan **2/2 activos** en todos. La
> policy `membership_wr` **no menciona `membership_historia`** (verificado sobre `pg_policy`): las
> filas falsas son **ruido, no privilegio**. El daño es a la **trazabilidad**, no al control.
>
> Esta advertencia se levanta cuando el rediseño cierre los dos bloqueantes, y **no antes**.

## Reglas de esta bitácora

1. **Se escribe durante el incidente, no después.** La reconstrucción de memoria pierde justo lo que
   importa: los horarios.
2. **"¿Hay evidencia de uso?" se contesta con `acceso_auditoria`** y con los logs del organismo o del
   banco. Si la respuesta es "no sabemos", eso mismo se escribe — y es un hallazgo sobre la auditoría,
   no un detalle del incidente.
3. **Un incidente no se cierra con "hay que tener más cuidado".** Se cierra con un control concreto,
   expresado como una regla verificable de ADR-0002 §B, con su número. Si no se puede expresar así, el
   incidente sigue abierto.
4. **Un secreto commiteado se considera público para siempre.** Se rota; reescribir el historial es
   opcional y posterior. Vale igual si el repo es privado.
6. 🔴 **«Ventana de exposición» y «¿Cerrado el CONTROL?» contestan preguntas distintas y pueden
   discrepar sin contradecirse.** La ventana dice **cuándo el defecto fue alcanzable**; la última
   columna dice **si existe el control que impide la repetición** (regla 3). Un incidente puede
   tener la ventana **abierta para siempre** y el control **cerrado**: es exactamente el #3, por la
   regla 4. **Lo que nunca se cierra retroactivamente es la exposición.**
7. 🔴 **Una fila cerrada se ANOTA, no se reabre.** Si un incidente posterior cambia lo que se sabe
   sobre uno ya cerrado —por ejemplo, degradando la evidencia con la que se cerró—, la corrección
   va **como anotación fechada dentro de la fila**, sin tocar el enunciado original. Reabrir
   borraría la cronología, que es lo único que esta bitácora no puede reconstruir después.
5. **Sobre el deber legal de notificar** (a quién, en qué plazo): **no tengo esa fuente cargada**
   (ADR-0002 §G, G-3). Lo que sí se hace siempre es notificar al cliente afectado y al titular del
   estudio, por deber profesional y contractual. Ningún plazo legal se afirma acá.
