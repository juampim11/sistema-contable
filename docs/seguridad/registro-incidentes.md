# Registro de incidentes de seguridad

> **Procedimiento completo: `docs/arquitectura/ADR-0002-seguridad.md` §E.4.** Este archivo es la
> bitácora, no el procedimiento.
>
> **Orden no negociable: contener primero, entender después.** Rotar o revocar en el minuto uno; no se
> investiga con el secreto vivo. Si es una credencial fiscal, la revocación puede requerir una acción
> **del cliente** ante el organismo recaudador: contactarlo es parte del paso 1.

## Incidentes

| # | Fecha | Qué se filtró / qué pasó | Ventana de exposición | Alcance (clientes) | ¿Hay evidencia de uso? | Acciones y horarios | Control que impide la repetición | Cerrado |
|---|---|---|---|---|---|---|---|---|
| **1** | 2026-08-15 | **Vulnerabilidad de aislamiento — exposición NO confirmada.** Escalada de privilegios por shadowing de `pg_temp`: 6 de las 8 funciones de `app` leían relaciones sin calificar el esquema, y `pg_temp` se busca primero aunque no esté en el `search_path`. Con la credencial ordinaria de la aplicación (sin superusuario ni `BYPASSRLS`) se anulaban `accessible_tenant_ids()` y `has_role_on()`, o sea **lectura y escritura sobre todos los tenants de la instancia**. Al caer `has_role_on`, **N2-R quedó al nivel de N2**. NO se expuso `credencial_fiscal.material_cifrado` (N3): su control es un grant por columna, que se evalúa antes que la RLS | **Desde `0001_tenancy.sql` (2026-08-10) hasta el fix (2026-08-15).** El defecto era fundacional: existió cada minuto de vida del esquema | 2 bases. `sistema_contable` (local, sintética). `sistema_contable_piloto`: los titulares reales bajo la excepción E-1, en tenants separados. *(Sin nombres, uuid ni identificadores en esta bitácora.)* «Es local» no baja la severidad: E-1 fija que el entorno local se trata como productivo a los efectos de los controles | **No se puede determinar** — y eso es un hallazgo, no un detalle (regla 2). `acceso_auditoria` se escribe **desde la aplicación** y el vector no pasa por la aplicación; Postgres no tiene triggers de `SELECT`, `pgaudit` está descartado (ADR-0000 §6) y `log_statement` está en el default. Lo que **sí** se verificó, antes y después del fix: `verificar_coherencia_path()` = 0 y cero membresías inesperadas en las dos bases ⇒ **no hay evidencia de escalada persistente** (≠ «no hubo acceso») | Encontrado por auditoría interna de un agente, revisando otra cosa. Reproducido 3 veces de forma independiente. Barrido de las 8 funciones. Verificación forense **antes** de parchear (el vector permitía una membresía real que habría sobrevivido al fix). `0015_search_path_pg_temp.sql` aplicada a local y piloto el 2026-08-15, hash verificado sin drift. PoC re-ejecutado post-fix por las dos vías | **R10 + R10 bis + R10 ter**, ADR-0002 §B.1, **reescritas dos veces el mismo día**. Final: la **primera** aparición de `pg_temp` es la **última posición** y hay ≥2 elementos, sobre `prokind in ('f','p')` de `app`/`public`, `definer` **e** `invoker`, con la exención nominada por esquema+nombre+aridad; ningún rol **no superusuario** conserva `TEMPORARY`; `app`/`public` son los únicos esquemas de dominio. **Dos versiones anteriores pasaron verde con el bug adentro**: la original («fija `search_path`» — las vulnerables lo fijaban) durante los cinco días de vida del esquema, y la primera corrección («**terminado** en `pg_temp`») durante un commit — PostgreSQL resuelve por **primera** aparición, así que `pg_temp, public, app, pg_temp` y `pg_temp` a secas terminaban en `pg_temp` y leían la trampa igual. Cerrado con **40 mutaciones**, 10 de ellas verdes indebidas al empezar, 0 al final, más `pg-temp-shadowing.test.ts`: la primera cobertura **conductual** del vector — planta la trampa de verdad, con control de vacuidad exacto, y se pone roja ante las 7 reversiones del esquema | **SÍ** — 2026-08-15. Código aplicado y verificado en las dos bases (PoC bloqueado por las dos vías, por separado) y control cerrado recién en la **tercera** redacción de la regla, la primera que sobrevive a la prueba por mutación. **La primera marca de cerrado fue prematura**: se puso con la regla ya escrita pero sin mutar, que es exactamente el error que este incidente documenta |
| **2** | 2026-08-15 | **Vulnerabilidad de aislamiento — exposición NO confirmada.** Ruptura del árbol de tenancía por edición manual de `tenant_node.path`. El trigger `app.rechazar_path_manual()` (`0001_tenancy.sql:131-144`) deja pasar la edición cuando el GUC `app.reparentando` vale `'on'` — y ese GUC es un *customized option* de namespace, que **cualquier sesión puede prender** con `set_config`, sin privilegio asociado. No es un control de autorización: es una bandera que el propio atacante enciende. La policy `tenant_node_wr` (`0001:322-327`) valida **quién** escribe y **qué padre** declara, nunca **el valor** del `path`. Como `app.accessible_tenant_ids()` resuelve el subárbol **por path**, un `path` fabricado mueve un nodo de subárbol a efectos de RLS. Es **H-1 reabierta por la puerta que el propio trigger dejó**: el comentario que precede a ese trigger describe esta misma fuga con todas las letras. Agravante: `app.reparentar_nodo()` aborta si el árbol queda incoherente; por esta vía el `path` corrupto **queda escrito**. 🔴 **Cae N2 y también N2-R** — y por un mecanismo distinto del #1: `has_role_on()` resuelve **por path** (`tn.path like mn.path \|\| '.%'`, `0001:233-246`), así que **no se anula: se satisface legítimamente**. El socio ajeno pasa a tener rol sobre ese nodo, con `cuenta_bancaria_identificador.numero` (CBU entero), `movimiento_origen_crudo.fila_origen` (la fila cruda, con CUIT de terceros que no son clientes de nadie) y `padron_socio_documento.documento`. **N3 no cae**, por la misma razón que en el #1: el grant por columna de `0002:114-118` no depende del árbol. **Y no es solo lectura**: `mov_crudo_wr` admite `socio` con `update`/`delete` —integridad del registro contable— y `membership_wr` le permite **crearse una membresía sobre el nodo empujado, que sobrevive a corregir el `path`**. Segundo orden: el `acceso_auditoria` del cliente empujado queda legible por el estudio ajeno, o sea que el rastro que existe para responder un incidente **se vuelve material del incidente** | **Desde `0001_tenancy.sql` (2026-08-10) hasta hoy — ABIERTA.** Fundacional, igual que el #1, y del mismo archivo | 2 bases, las mismas del #1. *(Sin nombres, uuid ni identificadores en esta bitácora; el PoC corrió en una base descartable con topología sintética, borrada al terminar.)* **Requiere rol `socio` o `admin_plataforma`** (un `contador` recibe `UPDATE 0`) — **y eso NO es una atenuante**: las filas 1 y 2 del modelo de amenaza de ADR-0002 §0 son insiders con cuenta legítima («estudio competidor», «empleado que se va»), o sea que el perfil es **la premisa**, no la excepción. Además `admin_plataforma` es un rol que H-9 ya tiene marcado para sacar de las policies de escritura. 🔴 **Y la contraseña de la credencial de aplicación está commiteada** en `.env.example`: mientras no se rote, el atacante no necesita comprometer una cuenta de socio, le alcanza con **conocer un uuid de socio** (`app.user_id` es un GUC que él mismo setea). Dirección **«push», no «pull»**, que **tampoco baja la severidad**: el sujeto protegido por el secreto fiscal es **el cliente**, no el estudio, y en push el damnificado es el único de los tres que no controla ningún control. Push es **superconjunto** de pull, no subconjunto: agrega escritura, agrega persistencia y **el estudio legítimo pierde acceso a su propio cliente**. La inversa (capturar un cliente ajeno) falla hoy por la coincidencia de tres cosas —`with check parent_id is not null`, **R15** (cada estudio es raíz ⇒ su path no tiene prefijo propio posible) y **`uq_tenant_node_path`**—, o sea que **ese índice único es hoy un control de aislamiento y en ningún lado figura como tal**: quien lo relaje «para permitir mover nodos» reabre el pull sin enterarse | **A diferencia del #1, acá SÍ hay mecanismo de detección** y dio limpio: `app.verificar_coherencia_path()` = **0 incoherencias en las dos bases**, medido el 2026-08-15 en el forense del #1 ⇒ **no hay evidencia de que se haya ejecutado** (≠ «no se ejecutó»). El vector deja rastro **observable mientras está en efecto, no indeleble** —un `path` que no coincide con el computado—; `verificar_coherencia_path()` detecta un **estado**, no un **evento**, y el estado es reversible. **Se suma la otra medición del mismo forense: cero membresías inesperadas en las dos bases**, y esa es la que cubre el residuo persistente de este vector. No es un agregado cosmético: revertir el push **no es simétrico** —tras empujar el nodo, el atacante lo pierde de su propio `accessible_tenant_ids()` y el `using` de `tenant_node_wr` ya no lo deja volver—, así que la variante «corrió y revirtió sin dejar nada» **exige un paso previo que sí deja fila**: una `membership` sobre el nodo. Lo que **no** hay es rastro del **intento** ni fecha. 🔴 Y **`tenant_node.updated_at` no sirve como evidencia forense**: en este vector el `UPDATE` lo escribe el atacante, que puede omitir la columna o poner lo que quiera | Encontrado por `tester` en la ronda de cierre del #1, atacando el estado post-fix — no por el barrido del #1, que buscaba otra clase de bug. Reproducido punta a punta en base descartable: el socio del estudio ajeno pasó de 1 a 3 nodos visibles y leyó `movimiento_bancario_crudo` del cliente empujado. Mecanismo confirmado contra el código de forma independiente. `verificar_coherencia_path()` re-corrido en las dos bases: 0. **Barrido de la clase entera** (¿hay otro control que dependa de un valor que el invocador setea?): en todo el repo hay **exactamente dos** GUC de namespace — `app.user_id` y `app.reparentando` — y **no hay un tercero**; `app.reparentando` no se setea nunca desde TS. La distinción que faltaba escrita: un GUC puede transportar **identidad** (`app.user_id`: no concede nada por sí mismo, todo sale de filas de `membership`, y ante valor ausente falla cerrado), **nunca autorización**. **Contención pendiente, no aplicada:** rotar la credencial de aplicación, y `revoke update on tenant_node from app_request` — medido: **cero** `update` sobre `tenant_node` en todo el TS, así que es privilegio que hoy nadie ejerce | **PENDIENTE.** Plan formal propio con `dba-data` + `seguridad-datos-financieros` + `security-engineer` — toca `tenant_node` y RLS, mismo criterio que el #1. Dirección en evaluación, sin decidir: (a) que la coherencia del `path` sea **obligatoria y no opcional** —validar contra el path computado del padre, de modo que ni con el GUC prendido se pueda escribir un valor incoherente—, o (b) que `reparentar_nodo()` señale su contexto con un mecanismo **no falsificable por el invocador**, en vez de un GUC abierto | **NO** — regla 3: no se cierra sin la regla verificable numerada en ADR-0002 §B |

## Reglas de esta bitácora

1. **Se escribe durante el incidente, no después.** La reconstrucción de memoria pierde justo lo que
   importa: los horarios.
2. **"¿Hay evidencia de uso?" se contesta con `acceso_auditoria`** y con los logs del organismo o del
   banco. Si la respuesta es "no sabemos", eso mismo se escribe — y es un hallazgo sobre la auditoría,
   no un detalle del incidente.
3. **Un incidente no se cierra con "hay que tener más cuidado".** Se cierra con un control concreto,
   expresado como una regla verificable de ADR-0002 §B, con su número. Si no se puede expresar así, el
   incidente sigue abierto.
4. **Un secreto commiteado se considera público para siempre.** Se rota; reescribir el historial es
   opcional y posterior. Vale igual si el repo es privado.
5. **Sobre el deber legal de notificar** (a quién, en qué plazo): **no tengo esa fuente cargada**
   (ADR-0002 §G, G-3). Lo que sí se hace siempre es notificar al cliente afectado y al titular del
   estudio, por deber profesional y contractual. Ningún plazo legal se afirma acá.
